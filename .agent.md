# HafiPortrait Photography System - AI Development Guide

This file provides comprehensive guidance for AI assistants working with the HafiPortrait Photography platform.

## Development Commands

### Core Development
```bash
# Start development server (port 3000, configurable via PORT env var)
pnpm run dev

# Development with specific configurations
pnpm run dev:docker          # Development on port 3002
pnpm run dev:public          # Development with public Socket.IO
pnpm run dev:full            # Development with WebSocket server
pnpm run dev:socketio        # Development with Socket.IO server

# Build and Production
pnpm run build              # Production build
pnpm run start              # Start production server
pnpm run lint               # Run ESLint

# Testing
pnpm run test:db            # Test database connection
# NOTE: test:storage is DEPRECATED (uses old storage-adapter)
# Use direct R2 testing via admin dashboard instead
```

### WebSocket/Socket.IO Servers
```bash
# WebSocket Server
pnpm run ws:start           # Start WebSocket server
pnpm run ws:dev             # Start WebSocket with nodemon

# Socket.IO Server
pnpm run socketio:start     # Start Socket.IO server
pnpm run socketio:dev       # Start Socket.IO with nodemon
pnpm run socketio:local     # Local Socket.IO development
pnpm run socketio:public    # Public Socket.IO (port 3003)
```

### Environment Management
```bash
# Environment status and setup
pnpm run env:status         # Check current environment status
pnpm run env:dev            # Development environment setup
pnpm run env:prod           # Production environment setup
pnpm run env:staging        # Staging environment setup
```

### Monitoring and Health
```bash
# System monitoring
pnpm run monitor:start      # Start enhanced health monitor
pnpm run monitor:check      # Run health check
pnpm run monitor:status     # Check monitoring status
pnpm run monitor:system     # Setup monitoring system

# Alerts and notifications
pnpm run alerts:send        # Send test alerts
pnpm run alerts:history     # View alert history
pnpm run alerts:stats       # Alert statistics
```

### PM2 Process Management
```bash
pnpm run pm2:start          # Start PM2 processes
pnpm run pm2:stop           # Stop PM2 processes
pnpm run pm2:restart        # Restart PM2 processes
pnpm run pm2:logs           # View PM2 logs
pnpm run pm2:status         # Check PM2 status
```

## Architecture Overview

### Tech Stack
- **Framework**: Next.js 14.2.15 with App Router
- **Database**: Supabase (PostgreSQL) with TypeScript interfaces
- **Storage**: Single-tier Cloudflare R2 (primary) + Google Drive (async backup only)
- **Real-time**: Socket.IO and WebSocket support
- **UI**: React 18 with Tailwind CSS, Radix UI components
- **Image Processing**: Sharp with automatic optimization
- **Deployment**: Docker, PM2, Railway/Render support

### Key Components

#### Database Layer (`src/lib/database.ts`)
- Centralized database service using Supabase
- Handles Events, Photos, Messages, and Pricing Packages
- Includes image optimization and compression analytics
- TypeScript interfaces for type safety

#### Storage System (`src/lib/direct-r2-uploader.ts`) - UPDATED 2024
- **ACTIVE**: Direct Cloudflare R2 uploader (single provider)
- **DEPRECATED**: Smart Storage Manager, storage-adapter.ts (throws errors)
- **PRIMARY**: Cloudflare R2 for all photo serving
- **BACKUP**: Google Drive async backup only
- Sharp image optimization with automatic compression

#### WebSocket/Socket.IO Integration
- Dual real-time communication support
- Configurable via `NEXT_PUBLIC_USE_SOCKETIO` environment variable
- Separate servers for different communication protocols

#### Admin System
- Comprehensive admin dashboard with mobile responsiveness
- Event management, photo management, and user analytics
- Real-time monitoring and system health checks
- Pricing packages management with drag-and-drop ordering

### Project Structure

```
src/
├── app/                    # Next.js App Router
│   ├── admin/             # Admin dashboard pages
│   ├── api/               # API routes
│   ├── event/             # Event-specific pages
│   └── demo/              # Demo pages
├── components/            # React components
│   ├── admin/             # Admin-specific components
│   └── ui/                # Shared UI components
├── lib/                   # Core libraries and utilities
│   ├── database.ts        # Database service (ACTIVE)
│   ├── direct-r2-uploader.ts # Direct R2 storage (ACTIVE)
│   ├── storage-adapter.ts # DEPRECATED - throws error
│   ├── database-with-smart-storage.ts # DEPRECATED
│   └── ...                # Other utilities
└── shared/                # Shared resources
```

### Tethered Shooting System
- **Location**: `tethered-shooting/` directory (NOT `DSLR-System/`)
- Python-based camera auto-upload system
- Automatic photo processing and R2 cloud storage
- Event management and monitoring scripts
- Integration with Direct R2 uploader

### Environment Configuration
- Multiple environment support (dev, staging, production)
- Environment-specific configurations in `.env.*` files
- Centralized environment management scripts
- Docker and CI/CD integration

## Development Workflow

### Local Development
1. Use `pnpm run dev` for standard development
2. For real-time features, use `pnpm run dev:socketio` or `pnpm run dev:full`
3. Environment switching via scripts in `scripts/development/`
4. Testing with `pnpm run test:all`

### Production Deployment
1. Build with `pnpm run build`
2. Use PM2 for process management: `pnpm run pm2:start`
3. Monitor with enhanced health monitoring: `pnpm run monitor:start`
4. Deploy via Docker, Railway, or Render

### Key Scripts by Category
- **Development**: `scripts/development/` - Environment setup and switching
- **Monitoring**: `scripts/monitoring/` - Health checks and system monitoring
- **CI/CD**: `scripts/ci-cd/` - Deployment and GitHub Actions
- **Utilities**: `scripts/utils/` - Various development utilities

## Critical Safety Notes

### ⚠️ DEPRECATED SYSTEMS (DO NOT USE)
```typescript
// THESE WILL THROW ERRORS - MIGRATION COMPLETED:
import { storageAdapter } from '@/lib/storage-adapter';
import { smartDatabase } from '@/lib/database-with-smart-storage';
import SmartStorageManager from '@/lib/smart-storage-manager';
// Error: "Smart Storage is deprecated. Use direct R2 uploader instead."
```

### ✅ ACTIVE SYSTEMS (USE THESE)
```typescript
// CORRECT IMPORTS:
import { directR2Uploader } from '@/lib/direct-r2-uploader';
import { database } from '@/lib/database';
```

### 📊 Database Schema (Key Fields)
```sql
-- photos table:
storage_provider: 'cloudflare-r2'  -- All new photos
google_drive_backup_url           -- Backup URL
storage_path                      -- R2 path
```

### Development Guidelines
- **TypeScript**: Strict mode enabled
- **Image Processing**: Sharp automatic optimization
- **Storage**: Single Cloudflare R2 + Google Drive backup
- **Mobile**: Comprehensive responsive design
- **Real-time**: Socket.IO or WebSocket support
- **Camera Integration**: Python tethered shooting system

### Environment Requirements
```env
# REQUIRED for storage:
CLOUDFLARE_R2_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET_NAME=

# REQUIRED for backup:
GOOGLE_DRIVE_CLIENT_ID=
GOOGLE_DRIVE_CLIENT_SECRET=
GOOGLE_DRIVE_REFRESH_TOKEN=

# REQUIRED for database:
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
```

### Storage Migration Status
- ✅ **COMPLETED**: Migration from Smart Storage to Direct R2
- ❌ **DEPRECATED**: Multi-tier storage system, storage-adapter.ts
- ✅ **ACTIVE**: Single Cloudflare R2 + Google Drive backup
- 🔒 **SAFETY**: Old system throws errors to prevent accidental use
- 🧹 **CLEANUP**: Updated package.json scripts and .env.example

### Critical File Paths
```bash
# ACTIVE FILES:
src/lib/direct-r2-uploader.ts     # Primary storage handler
src/lib/database.ts               # Database service
tethered-shooting/                # Camera system (NOT DSLR-System/)

# DEPRECATED FILES (DO NOT MODIFY):
src/lib/storage-adapter.ts        # Throws error
src/lib/database-with-smart-storage.ts # Throws error
src/lib/smart-storage-manager.js  # Deprecated
```